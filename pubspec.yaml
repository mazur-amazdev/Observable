name: dart_subject
description: Used for pumping data through different contexts
version: 1.0.2
repository: https://github.com/mazur-amazdev/Observable

environment:
  sdk: ^3.1.3

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
